# Delivery-discovered stopped console fix — 2026-09-12

The first actual 320×578 original-project delivery run completed the five opening choices but could not click a destination: stopped scenery pushed the console and dock below the viewport. This was a new observed delivery blocker, inherited from the August fitter and narrow-screen CSS; the earlier modal/overlay QA did not exercise this fresh first departure.

`layout-samples.json` proves the feedback cause. The initial stage was 250px, route content had scrollHeight404/clientHeight208, the console bottom was470px and dock top502px. The height-filling console shrank when the fitter grew the scenery, leaving the same32px margin-derived gap. Successive passes grew the stage250→279→308→337→366px. Opening result renders repeated the same adjustment until stage735px, dock bottom818px and console height0. `pre-departure-dom.json` records the final stopped geometry. No browser viewport mismatch or dock-toggle cause was found.

The only runtime change is six lines in the existing `scheduleStoppedStageFit` owner: when an active route/local console already needs scrolling, do not take space from it to grow scenery. Wide layouts with content that fits retain their existing gap fitting. No CSS, engine, story, assets, save format, progression or thresholds changed.

RED: the new `tests/test_stopped_stage_fit.py` completed a real fresh opening and failed `dock bottom <= viewport height + 1` with818>579. GREEN: the same test completed all five opening choices, accepted the mission, switched local/route modes and clicked the real Busan→Yangsan destination at320×578,360×700,390×844 and1440×900. The320 stage remains250px and route console208px high. It uses no grants, forced clicks or CSS patches. Existing `python3 tests/test_golden_route.py` also passed every check, including stopped console spacing, resize, first leg and Miryang controls, with zero console errors. Exact commands, logs and hashes are in `verification.json`.

The new worktree HTML is77,667,306 bytes/SHA-256 `b6e9220728359658721ad8c4a52cb5069e94ace0d42a2b67bf4d4f5cb5bfdb56`. GAME_BUILD remains `2026-09-11-director-journey`. Task8's15 qualified campaigns remain evidence from the earlier77,666,839-byte/`1c560199…9d07` runtime; they are not relabelled as new-hash campaigns. Current exact-response original-project first-departure and earned-finale/reload proof follows after independent scoped approval and resynchronization. Original source and intermediate HTML/AIT receipts are still preserved at this report point.
