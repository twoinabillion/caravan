# Task 3 — authored main-story evidence

Implemented from accepted Task2 HEAD `3cd613045f255e32388f23cfbd0ea2e30e3cae86` in the isolated director worktree. No optional companion story is now an undisclosed completion requirement. Two fresh live campaigns reached the actual Seoul gate without any companions, `es_truth`, resource grants or evidence injection.

## Implementation and authority

- `src/03k-main-evidence.js` adds three resolved scenes: three named transfer recipients correct and sign their testimony; the command archive is compared against their numbers and the parents' order; three named network identities explicitly confirm their operational support over the mother's relay. Each grants only its own `main_*` evidence flag and journal note. All three are `noPool`; missing evidence is deliberately obtainable rather than a random encounter.
- The command archive identifies the missing human confirmation, Cheollian's own decision to classify the parents' human-confirmation amendment as a threat and generate their family transfer order, and the separate unnamed upper receiving line. It does not disclose Eunsu's personal confession or write `es_truth`. The purpose of the original upper condition remains unknown.
- `src/04g-engine-evidence.js` is the single owner of `G.pillars`, `G.mainStoryReady`, `G.seoulReady`, `G.seoulMissing`, and `G.departureSteps`. Previous owners were moved, not appended or shadowed. `G.mainEvidenceRows()` supplies required completion facts and their recovery events; departure rows consume that same contract.
- `G.mainEvidenceOpportunity()` exposes the first unresolved record's event, map target and exact player action. `G.exploreStatus()` and `G.explore()` open that record deterministically for 30 game minutes. This does not consume a salvage attempt or give salvage resources. A normal day's exhausted/random searches cannot permanently hide a mandatory document. Evidence still changes only when the player resolves the authored choice.
- Earlier record opportunities are at Yangsan, Gimcheon, Cheongju, Cheonan and Pyeongtaek. Suwon's northern exchange is the recovery point once the player is at Suwon/Seoul or beyond 370 km. Distance changes only the place to recover missing material, never evidence. Mother/recent-signal/father chains reopen their appropriate unresolved stage. No save migration writes progress.
- `G.questNavigationPlan`, main quest entries, the departure checklist, and current-goal detail use the same actionable target/hint. A player at Seoul with missing material sees preparation and a route back to Suwon, rather than a falsely available inner-Seoul objective. Explicitly tracked side missions retain their navigation priority.
- Existing personal arc and recovery evidence remains valid. Three completed companion deeds can supply the testimony alternative; the authored three-recipient scene supplies the main alternative. Full-crew personal payoffs and deed flags are unchanged. Legacy `es_truth` plus existing truth evidence still works; the new verified command record is an alternative. The recovered key and signed testimony copy provide the two physical records on the direct main route.
- `D.coreLinkedCells(state)` / `G.coreLinkedCells()` returns the union of original `cell_*` contacts and the three confirmed relay identities (`road`, `ghost`, `mountain`). A remote confirmation and a previous visit cannot count twice. Final decision requirements and their displayed contact names use this union; `G.cellsLinked()` retains its original meaning of personally encountered contacts.
- `mother_broadcast_ready` is now explicitly required with `mother_reunited`. The real mother-truth outcomes have always written both; artificial old test fixtures supplied only one and were corrected to represent the real outcome. The father remains dead and the mother remains outside Seoul at the relay.
- Task1's premature completed journal seeds are now restricted to explicit legacy entry modes. The interactive bus/module/appeal choices write their own notes at resolution. The opening checklist also uses future actions until those choices are resolved. The opening regression now expects the actual resolved `도윤 가족의 버스` note, not the premature generic seed.
- Current goal copy no longer says four companion stories are required. No active objective card gained an outcome/reward preview.

## Required evidence and real entry paths

| Requirement | Authored writer / path | Main recovery target | Existing alternative |
|---|---|---|---|
| `first_order_trace` | `onboarding_first_road` choice, normally first road | Yangsan; later Suwon | Existing completed flag |
| split / `parents_routes_traced` | `parents_diversion_manifest` then `parents_separated_work` | Gimcheon then Cheongju; later Suwon | Completed route trace accepts legacy saves missing separate split flag |
| `parent_key_found` | `story_family_key` locates only; `story_personal_cache` chains to shared or guarded real extraction | Cheongju; later Suwon | Existing recovered key; locating alone never suffices |
| three human testimonies | `main_transfer_testimony` → `main_testimony_record` | Cheongju; later Suwon | Three completed personal deeds |
| verified command/accountability | `main_command_ledger` → `main_command_record` | Cheonan; later Suwon | `es_truth` from `es_backdoor` or `ev_eunsu_past`, plus existing three truth facts |
| `father_fate_known` | `history_failed_namsan` → `parents_father_last_log` | Pyeongtaek; later Suwon | Existing completed flag |
| mother and broadcast | `history_parents_network` → `parents_mother_reunion` → `parents_mother_truth` | Suwon | Both completed flags |
| three operational contacts | `main_relay_conference` → `main_relay_confirmed` | Suwon | Three or more existing resistance cells |
| two kept records | real key + signed testimony copy | Key/testimony opportunities above | Original recovery deeds (`postman_letter`, `gp_envelope_found`, `coffee_found`, key) |

The baseline writers and geographic restrictions remain documented in `artifacts/director-pass-2026-09-11/progression-audit.md` and `evidence-reachability-baseline.json`. In particular, the old resistance reveal, coastal/Daegu contacts, party-only grandfather envelope and Eunsu-only truth writers remain optional alternatives; none is silently granted.

## Red / green evidence

All browser commands ran in isolated headless Chromium, outside the macOS sandbox only because Chromium's MachPort bootstrap is blocked there. No personal profile or save was used.

- `python3 tests/test_director_progression.py` initially produced **11 expected behavior failures**: missing authored scenes, main/Seoul readiness, checklist agreement, preparable decision, actionable missing navigation, Seoul recovery, saved partial evidence, required broadcast, late recovery, and premature journal notes. Three checks already passed. The preliminary attempts were harness setup failures (sandbox Chromium launch, then a corrected `G.req` → `G.reqOk` typo), not claimed as red evidence.
- Mobile QA exposed an unknown `???` speaker for the player's testimony request. A rendered-scene assertion was added and failed, then explicit authored turns fixed it. The sender is now the player's actual name. Relay/record quotations are narration instead of guessed anonymous speakers.
- A further opening-checklist test failed because its first three descriptions spoke in completed tense before the scenes. Conditional descriptions fixed it.
- Final `python3 tests/test_director_progression.py`: **21/21 PASS**, including distinct identity union, deterministic search after ordinary daily attempts are exhausted, real 30-minute cost, no evidence on merely opening a scene, save/load retention, legacy evidence, distance-no-grant, no premature facts, named rendered speech, and actual testimony choice resolution. The exploration fixture explicitly activates the gameplay surface with the existing QA restore API; its initial inactive-title-screen failure was test setup, not engine behavior.
- `npm run build:html --silent`: PASS. New data module is included in dialogue lint and content validation as well as the build. 994 validated events; existing 58 legacy asset warnings and the existing ~76 MB HTML size advisory remain. No new raster assets.
- `python3 tests/test_source_health.py`: PASS, standalone parsing, source budgets and fresh build. Core shrank below its 1200-line limit; no limit raised.
- `python3 tests/test_parent_arc.py`: PASS after the artificial ready fixture includes the real mother broadcast flag; all actual parent chains and canon checks unchanged.
- `python3 tests/test_seoul_sequence.py`: PASS after the same fixture correction; five stops and all three outcomes complete, including day 120.
- `python3 tests/test_director_opening.py`: PASS after expecting the actual bus-resolution note. Regenerated Task1 screenshots were restored rather than included as unrelated drift.
- `python3 tests/test_finale.py`: PASS (conditional decisions, dissent costs, endings).
- `python3 tests/test_save_migrations.py`: PASS (historical/partial/corrupt saves).
- `python3 tests/test_journey_beats.py`: PASS; geographic/resource/companion guards and one-at-a-time dequeue retained.
- `python3 tests/test_director_story_coherence.py`: PASS.
- `git diff --check`: PASS.

One inherited failure is **not** called green: `python3 tests/test_goal_bag_alignment.py` times out at line 61 waiting for obsolete `#ovl-status` navigation. Reproduced identically on an untouched temporary build of Task2 HEAD at `/private/tmp/caravan-task3-baseline` (`git archive HEAD src tools tests docs reports`, shared read-only asset symlink, `node tools/build-html.mjs`, then the same test). Task3 does not weaken UI or story guards to satisfy this stale harness. Task8 owns that harness update.

## Actual gameplay and captures

Commands (existing live server, no `rev` parameter):

```
python3 tests/probe_director_progression.py --limit 2500
python3 tests/probe_director_progression.py --limit 2500 --main-record-route --output artifacts/director-pass-2026-09-11/task3-conference-route.json
python3 tests/capture_director_evidence.py
```

Both campaigns start through name/new-game UI and the complete five-choice interactive opening. They use actual choice/result controls, normal travel/search/camp/trade/repair/rescue actions and finite resources. Dialogue reveal and engine ticks are accelerated; these are functional reachability traces, not human reading-time or enjoyment evidence.

- `task3-main-route.json`: **95 actions, 430 km, day 7, zero companions, 47 used events, zero page errors**. Three ordinary cell encounters were preserved as valid legacy alternatives. A finite-fuel rescue occurred naturally. Ready at actual Seoul gate.
- `task3-conference-route.json`: **91 actions, 430 km, day 5, zero companions, 46 used events, zero page errors**. Skips searches at Busan, Daegu and Mungyeong. **No `cell_*` flags and no `es_truth`**. Plays all three new scenes; direct contacts come only from resolved `main_relay_confirmed`. Ready at actual Seoul gate.
- Common observed evidence sequence: Gimcheon 170 km split record; parent joint-work beat 248 km; Cheongju 300 km module location/extraction/testimony; Cheonan 338 km command ledger; Pyeongtaek 366 km failed entry/father; Suwon 396 km recent mother signal/reunion/truth, plus relay conference in the second run. The ordinary automatic beat queue was not weakened; deterministic explicit search prevented mandatory evidence starvation.
- Both full trace JSONs and their generated game saves are preserved. These are newly generated QA saves, not personal user saves.
- `task3-captures/seoul-gate-390.png` is the actual campaign gate capture. During those runs the probe also called `G.seoulEnter` just before the already pending natural gate event; this applied no effects or evidence, and the natural gate event is what the capture displays. The committed runner now simply waits for the natural gate instead. These traces prove arrival/readiness; they do **not** claim the inner Seoul sequence was played through.
- `task3-current-ui/` contains six **current live read-only record replays** (390×844 and 320×578), plus `checks.json`. They load the earned second-campaign save and reopen its already-used record scenes without applying choices or changing any evidence flags. They verify current speaker/layout fixes; they are distinctly labelled replays and retain the saved Seoul background, not new campaign evidence. Choices remain visible/reachable, no horizontal growth, no unknown speakers, no page errors. Representative current testimony, ledger and relay crops were visually inspected. Earlier automatic campaign record screenshots predate the speaker correction and are not offered as final UI captures.

## Interfaces and remaining work for later tasks

- Task5 can pace automatic beats around explicit evidence stops without making mandatory documents compete in the random pool. Main search presently takes 30 minutes and is deliberately separate from salvage limits. No automatic priority/threshold changes were needed for the demonstrated route. Both full campaigns used the ridge branch; the western branch remains for Task8's matrix.
- Task6 can assign location/cast-appropriate art to the new records if desired; currently they use the existing contextual background and authored text. None of the controller's seven unwired art candidates is part of this commit.
- Task7: `D.coreLinkedCells(S)` / `G.coreLinkedCells()` is the contact union to use in finale descriptions and requirements. Transfer requires three unique confirmed contacts and is legitimately available to the solo main route. Sleep still needs four holders (healthy companions plus unique contacts); quarantine still needs three healthy companions with mood ≥45. A main route can add a real fourth contact/healthy companion for sleep, or recruit and support a real crew for watch duty. No extra holders/watch people are invented. Further method-specific operational preparation, solo finale prose, fixed cast art, omitted character reactions and sleep/record availability payoffs remain Task7/Task6 work.
- Controller continuation found an inherited gate-save edge: reloading the earned save while `seoul_open` is still awaiting its gate choice resumes at Seoul/stage 0 without reopening that gate. `enterGame` only restores Seoul after `seoul_open`. The records persist, but automatic pre-choice gate resume is **not** claimed; controller assigned this to the Task7/8 finale/save pass.
- Task8: complete both route branches and the three actual endings with prepared campaign states; repair the baseline goal/bag fixture. Current source checks are functional regressions, and Seoul-sequence completion is fixture-based, separate from the no-grant gate runs.
- HTML was rebuilt from source; final AIT packaging belongs to the integrated whole-journey delivery. No physical-device/performance or fresh-player fun claim is made.

Self-review checked event writers, legacy alternatives, deduplicated contact identity, missed/partial save recovery, exactly one owner per moved method, contextual opening copy and source scope. No unresolved Task3 implementation blocker. Remaining inherited harness and later-task coverage limits are explicit above.

## Review fix round 1 (base `2d517d9dc5ebf43bb32f1e980075a00bd92b414a`)

The reviewer found three important gaps and one minor copy inconsistency. This section supersedes the earlier claim that merely pointing all old events at Suwon was a fully truthful recovery path.

1. **Actual northern recovery, not the original southern scene.** `src/03l-main-recovery.js` registers nine explicit `main_recovery_*` events. The first record is now a transferred paper copy/stored module or a new remote query at the Suwon exchange. Split/joint-work/failed-entry/father records explicitly identify their copied northern provenance. The module location scene inspects the player's own van parked outside the exchange. The cache is a transported, logged and sealed physical box; it is not freshly found under a southern bus desk. Both extraction branches receive the original missing pages through the exchange and still physically detach the real key from the van. Father's optional number plate is explicitly a new plate engraved from the copied number, not a found original artifact. New choices/results/notes say where and how the material was actually obtained. Recovery uses the existing contextual Suwon background, not the original first-overpass image.

   Reviewed every remaining reachable source as well: the three-civilian testimony is a present exchange encounter and does not claim an earlier location; the command archive is a portable/current northern record; `history_parents_network` already takes place at the northern exchange; reunion and mother truth already describe the nearby Seoul-outskirts relay. Those source scenes remain valid. Mother is still physically met and still stays outside Seoul. Main civilian/command/relay flags are never assigned merely by choosing a recovery target.

   `D.mainRecoveryEvents` maps source IDs to stable, save-resolvable recovery IDs. Source choice effects are copied with their costs/flags/items intact; only provenance text/notes and recovery-chain destinations change. `G.mainEvidenceEventDone` prevents already-established main facts from subsequently replaying an untouched original through either the beat queue or story queue. It does **not** insert the missed original into `S.used`. Partly finished shared/guarded extraction now resumes the appropriate stage.

2. **Honest full cost.** `G.mainEvidenceChoiceTimes` calculates the available choice/chain time range. The map/goal hint now says `열람 준비 30분 + 선택·후속 작업 10~35분` for the first record; each northern recovery choice also shows its own additional minutes. Existing 20/35/10-minute effects were not waived. Full UI resolution costs 50/65/40 minutes respectively, and remote lookup still adds pursuit 1. Zero-additional-time evidence scenes continue to charge the actual 30-minute preparation cost. No broad cost/pacing tuning was done.

3. **Hybrid evidence uses its real source.** If completed companion deeds supply testimony and there is no `main_testimony_record`, the opportunity opens `main_command_companion_ledger` and `main_relay_companion_conference`. The command scene names the actually completed companions, uses their directly heard stories from the journey record, and separately checks the parents' order number against the command original. It no longer invents three civilian order numbers or signed copies. The relay carries those companion accounts; its result/note records an operational response sheet, not civilian testimony papers. The existing civilian scenes remain the branch for the actual three signed civilian records. Both branches write the same verified command/contact flags and preserve the identity union.

4. **Legacy split copy.** `G.parentSplitKnown()` supplies the shared `parents_split_known || parents_routes_traced` predicate to readiness, completed checklist text and replay suppression. A legacy completed route now says the separation was confirmed, rather than telling the player to find the schedule again.

### Focused red/green and live evidence

- `python3 tests/test_director_evidence_review.py` initially failed all six source/place/cost/hybrid/legacy checks on the reviewed implementation. Final run: **13/13 PASS**, including full UI resolution for all three first-record choices, preservation of pursuit, truthful notes, no fabricated original encounter, mapped extraction chains, and suppression of already-resolved original beats/story entries.
- `python3 tests/test_director_progression.py`: **21/21 PASS**; its late-recovery assertion now checks source identity plus the explicit northern event ID.
- `python3 tests/test_journey_beats.py`: PASS; this is the only extra engine suite rerun because the focused replay suppression touched its queue guard.
- `npm run build:html --silent`: PASS, 1005 validated events. New recovery data is registered in build, dialogue lint and content validation. Existing asset/size advisories remain.
- `python3 tests/test_source_health.py` and `git diff --check`: PASS.
- `python3 tests/capture_director_evidence_review.py`: PASS against `http://localhost:4176/game?caravan-live=1`. **Explicit late-save and completed-companion fixtures**, actual search/choice/result controls, finite resources, no grants after setup. These do not replace or masquerade as the two existing fresh no-grant campaigns. `artifacts/director-pass-2026-09-11/task3-review1/trace.json` contains before/after states, complete rendered entry/result text, hints and actual notes. Nine 390×844 captures show source paragraphs, choices and results for northern remote lookup, hybrid command and hybrid relay. Place stays Suwon, remote lookup takes 40 total minutes/pursuit +1, each hybrid operation takes 30 minutes, both hybrid evidence flags resolve while `main_testimony_record` stays absent, and the original first-road ID is never marked seen. Zero page errors/unknown speakers. Source/choice/result crops were visually inspected.

Existing fresh solo campaign traces/captures are retained. The controller independently continued a fresh finite-resource solo campaign through the ending; that is separate evidence and not claimed as this fix's work. The first captures incorrectly froze result-pill animations because the fixture setup used `UI.restoreQaView`, which adds `qa-exact-replay`. The controller diagnosed this harness artifact. The corrected capture script removes that freeze after setup and waits for every nonempty reward pill to reach visible opacity before capturing. Final captures show actual result chips; no game styling was changed. No broader regression sweep, raster changes, controller audit edits or art-candidate wiring was included.

## Review fix round 2 (base `3434e903f44898e344a1c3b5ec0ba07939e6f697`)

The second review accepted all four original fixes and found one remaining important path: persisted `_chain` IDs bypassed the beat/story guards in `UI.closeEvent`. Closing also saved the old chain before clearing it, allowing another reload to retain that stale link.

- `D.mainEvidenceCompletion` now includes both shared/guarded extraction sources. `G.mainEvidenceSourceId` normalizes recovery IDs to their source; `G.mainEvidenceEventDone` uses that same source identity for both variants, including queue guards.
- `G.resolveMainEvidenceChain` handles the actual chain consumer. Completed main evidence is discarded without reopening or awarding anything. At **actual `S.at === 'suwon'`**, an unfinished source/recovery ID opens its northern variant. An original ID can still continue at its evidence row's original target (Cheongju for extraction). At another location, or if a Suwon recovery ID has travelled elsewhere, the stale link is discarded and existing partial flags remain; the explicit missing-evidence target/search can resume the work. Total kilometres never make a Suwon scene appear in Cheonan. Unrelated chains pass through unchanged.
- `UI.closeEvent` resolves and clears the consumed chain before its normal save, then opens only the accepted event. Camp/opening sequencing retains its prior priority. The existing missing-evidence navigation owns deferred work. No new pending-event serialization or finale-resume behavior was added, no completion flags/items were granted by routing, and the inherited pre-choice Seoul gate resume remains Task7/8 scope.
- The saved-chain bypass cause and prevention rule are recorded in `docs/DEVELOPMENT_GUARDRAILS.md`.

Verification was limited to the changed contract and existing recovery/save checks:

- `python3 tests/test_director_saved_chains.py`: **RED 12/12 failed** on the reviewed build (old/wrong-place scenes opened and the saved chain remained); **GREEN 12/12 passed** after the fix. Both shared/guarded branches cover persisted original and recovery IDs, completed/partial states, Suwon, high-km Cheonan and original-target Cheongju. The test performs a real save/load, UI choice/result close, and checks the rendered event, unchanged key/partial facts, consumed persisted link and reachable missing target.
- `python3 tests/test_director_evidence_review.py`: **13/13 PASS**. `python3 tests/test_save_migrations.py`: **PASS**, including legacy/partial/corrupt saves and zero runtime errors. `python3 tests/test_source_health.py`: **PASS**. `npm run build:html --silent`: **PASS**, 1005 validated events; existing 58 asset warnings/size advisory unchanged. `git diff --check`: **PASS**. Initial sandboxed Chromium launch was denied by macOS bootstrap; authorized isolated-browser execution succeeded. One initial harness attempt used an unavailable close icon; it was corrected to the actual choice/result button before recording the RED run.
- `python3 tests/capture_director_saved_chain.py`: **PASS** on existing live4176. An explicitly seeded partial save is actually reloaded; resolving a current exchange testimony via UI consumes its saved extraction chain. The northern extraction entry remains incomplete until its choice, then awards the real key, clears the partial flag and survives another reload without the chain. This is a focused saved-state fixture, **not** fresh-campaign evidence or a claim that generic pending result modals restore. No resource/evidence grants occur after setup. Both authored choices used here have zero additional time; no existing costs were waived. `artifacts/director-pass-2026-09-11/task3-review2/trace.json` preserves before/pending/result/persisted states and text. Three 390×844 source/choice/result captures were visually inspected: correct Suwon context, readable controls and visible nonempty reward chips. Normal live reload was used, without the QA animation freeze. Zero page errors/unknown speakers.

Self-review found no remaining Task3 blocker. Existing solo evidence and round1 captures are retained; no controller art/audit/context files or generated CURRENT/asset-budget drift are included.

## Controller clarification after acceptance
The earlier wording “obsolete #ovl-status navigation” was an unproven explanation of the baseline goal/bag timeout. Current code still opens #ovl-status. Only the baseline reproduction is established; Task8 will diagnose startup/skip/reset/timer behavior and preserve useful alignment coverage. See artifacts/director-pass-2026-09-11/smoke-baseline-triage.md. Task3 remains accepted; this correction changes no source or test result.
