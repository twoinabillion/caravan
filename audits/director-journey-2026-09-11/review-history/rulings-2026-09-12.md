# 통합에 적용되는 컨트롤러 판정 — 2026-09-12

이 문서는 전달 시점에 필요한 판정의 스냅샷이다. 출처는 `.superpowers/sdd/2026-09-11-director-journey/progress.md`와 `task-9-context.md`이며, 전체 문맥은 같은 폴더의 [`progress-ledger-2026-09-12.md`](progress-ledger-2026-09-12.md)에 보존한다.

1. **원본 보존.** 원본 `/Users/sang/caravan`의 기존 변경 26개는 기준 커밋 `73bef1d73df79fe4d7342182488c64a0f37dc600`과 967개 해시 ledger로 보존한다. Phase B 직전에 해시와 새 경로 충돌을 다시 검사하며 reset이나 blanket copy를 하지 않는다.
2. **두 단계 전달.** Phase A는 격리 작업트리의 검증·문서·감사만 커밋한다. 독립 Task 9A review와 전체 브랜치 승인이 끝난 뒤에만 Phase B가 원본을 백업·통합·빌드한다. Phase A에서 Task 9 완료를 선언하지 않는다.
3. **영구 이벤트 집계.** `D.events`, 서울 단일 이벤트 3개, 서울 정류장 5개, `D.onboardingMission`, 부산 출발 5개를 포함하고 런타임 야영 대화 identity는 제외한다. 중복 ID와 구조화 턴의 종류·본문·화자를 검사하며 opening ID 예외나 기준 완화는 허용하지 않는다.
4. **동결 런타임 증거.** Task 8 증거는 `GAME_BUILD=2026-09-11-director-journey`, HTML 77,666,839 bytes, SHA-256 `1c5601994277f1603e0d50c164fdab432b561d4c9d0700bbe07c867c78de9d07`에 고정한다. Phase A의 검증·문서 변경 뒤에도 이 바이트가 같아야 한다.
5. **기존 경고와 용량.** 58개 시각 경고 문자열을 정확히 보존·비교한다. 32,000,000-byte 권고와 80,000,000-byte 상한을 낮추거나 예외 처리하지 않는다.
6. **증거의 종류.** 두 경로와 여섯 영입은 새 게임 정상 자원 증거, 세 결말은 101,104-byte 실제 획득 체크포인트의 분기다. 역사 저장 fixture와 동료 성장 fixture는 진단이며 최종 캠페인 수에 합산하지 않는다. 223 PNG와 3 journal export가 226개 색인 파일이다.
7. **사람 경험의 한계.** 코어 37턴, 에필로그 도입 10턴, 여섯 동료와 저장 기억이 있는 방식별 후속 최대 29턴은 실제 UI 턴 수다. 읽기 시간·재미·애착·결말 보상 또는 휴대폰 성능 측정값으로 해석하지 않는다. 사람 증거 전에 새 페이싱 개편을 미리 정하지 않는다.
8. **필수 artifact 예외.** 원본 전달에 필요한 artifact는 역사 fixture 세 개, `legacy-fixtures.md`, 정확한 Task 8 earned checkpoint 한 개뿐이다. 대량 Task 8 원본은 작업트리에 보존하고, compact index와 대표 이미지 8장은 최종 audit 안에서 전달한다.
9. **AIT payload 확인.** `check-release.mjs`의 HTML build 확인만으로 AIT 내부 게임을 확인했다고 쓰지 않는다. 새 AIT의 모든 index entry를 한 번의 unzip으로 검증하고 `web/*`를 새 `dist/web/*`와 대조하며, `GAME_BUILD`는 AIT의 `web/index.html`에서 확인한다.
10. **소유 범위.** Root가 director/Obsidian memory, 독립 review, 전체 승인과 최종 완료를 소유한다. 로컬 전달만 승인돼 있으며 push·배포·외부 메시지는 범위 밖이다.
