# Task 6 — truthful scene art and mobile hierarchy

**Review round1 correction:** the initial three opening captures were invalid due to a delayed Seoul chain. Their opening-verification claim is withdrawn and superseded by the identity-asserted fresh-page evidence below. Current aggregate inventory has69 states; only the12 review paths were freshly rerun in this round.

Continued the existing uncommitted Task6 work from accepted base `6cb0180e895a09200bd43db0e9a0ecd085651e83`. Preserved all seven previously inspected candidates, two later empty-cast edits and inherited before screenshots. No Task7 finale writing/save behavior, original workspace or personal browser was changed. Verification-before-completion skill applied.

## Concrete mismatch inventory and decisions

| Exposure | Observed mismatch or risk | Final treatment |
|---|---|---|
| Opening bus departure | Narrow third action partially below initial viewport; next action hierarchy must remain usable | Existing scrolling/choice controls preserved and inspected at320/390/1280; no new opening art needed |
| Recruitment, Leo rescue | Before rescue must keep Bori isolated and Leo on the dry side | Reused canonical `recruit-leo-rescue-choice-v1`; actual before crop verified |
| Road postman / ghost courier | Old courier art pictured fixed companions and dog on solo journeys | `event-postman-solo-v2` depicts only canonical postman, bicycle/list and Dalguji; all parties valid |
| Ridge rescue/anchor/extract | One rescue image implied a particular rescued count through all phases | `route-ridge-rigging-v2` keeps people off camera; rope/cart carries the risk without a false count |
| Ridge result | Cargo and injured/success states differ, but all six outcomes save four people | All six results use `route-ridge-safe-v2`: three unrelated adults and child on safe road, no medicine/cargo claim |
| Walker strike | Generic success includes distraction that leaves the machine active | Only crossbow/pipe/Kangwoo disabling successes use `combat-walker-disabled-v2`; failures, distraction and retreat retain active walker |
| City exchange records/recovery | Contextual fallback moving-road art mislocated current record reading | Reused existing `parents-linked-records-v2` / `parents-diversion-record-v2` detail art on explicit evidence/recovery events |
| Seoul core / keys / testimony | Fixed group scenes invented companions in solo/partial parties | Empty point-of-view `seoul-core-view-v2` across stages/choices; speaker portraits remain authored |
| Seoul night / result | Fixed camp cast and personal props invented party/items | Empty `seoul-night-quiet-v2`, then neutral Dalguji `seoul-home-dawn-v2`; no dog, guitar or optional keepsakes |
| Uplink / reset | Existing cliffhanger scenes pictured a fixed party | Canonical empty-cast edits preserve white local-core vs distant red array, then dark core vs independent red terminal |
| Ending heading | Narrow title split 천리안 midword | Owning heading uses keep-all with safe emergency wrap |

Nine v2 WebPs registered with scene descriptions, formats, sizes, canonical reference lists and provenance. General scenes1024×576; Seoul cinematic scenes1280×720. Existing old art remains available; no generated HTML hand edits. PNG masters remain locally under `artifacts/director-pass-2026-09-11/scene-masters/`; all nine text provenance notes are committed under `audits/director-art-2026-09-11/provenance/`. The last two exact prompts/reference lists were recovered from the preceding worker's two relevant tool records, not reconstructed. Both gallery captures preceded wiring; seven earlier gallery inspections are documented in their preserved notes.

## UI and ownership

Modified the existing `combat-stable-command-layout-20260826` owner: removed redundant report border/shadow/framing, recovered horizontal reading room, and gave the outcome dock one auto-sized grid row. The `.on` selector specificity is essential: old three-row containment otherwise stretches the footer. Final live button is48px with49px dock. Existing decision choice scrolling, costs and detail control remain.

The tutorial-note owner now reads theme variables supplied by the combat owner, making its body/label readable on the dark field report. Paper story reports override their own faded-text variable so secondary outcome text is brown on tan rather than pale gray. No appended emergency CSS blocks. Deferred Task5 wording now reads `구미 공단 폐허까지 이동한다`; route planning is unchanged and the exact phrase is asserted.

## Validation

- `npm run build:html --silent`: PASS,1005 events, unchanged58 inherited visual warnings; 77,653,489 bytes final; exact bytes in committed `audits/director-art-2026-09-11/build.log`, under80MB hard limit. Existing32MB recommended-size warning remains.
- `npm run audit:scene-assets --silent`: PASS,502 scene-class assets and73 portraits, canonical reference files present. All nine new files meet contract geometry. No strict-mode pass is claimed. Exact58 inherited warnings are retained in `asset-validation-expanded.log`; only display truncation was removed in a temporary diagnostic copy, not any validator rule.
- `python3 tests/test_source_health.py`: PASS, source parsing/size exceptions and fresh build. Committed log preserved.
- `python3 -m pytest -q tests/test_director_art.py tests/test_director_choice_consequences.py tests/test_director_cities_routes.py`:13 passed in23.33s. Subsequent focused art test with added finale/particle assertions:1 passed in2.07s. Covers all six ridge result mappings, disabling-vs-distraction/retreat mapping, solo core/night/postman, recovery-record scenes, existing choice costs/branches and route/city regressions.
- `python3 tests/capture_director_art.py`: PASS,63 settled captures; all three viewport widths have no horizontal overflow and49px combat result dock. Full inventory in `after/trace.json`; camp-only final recapture waits for natural toast dismissal.
- `python3 tests/test_event_typography_layout.py`: PASS,18 states; text alignment, frame containment, contrast and choice scroll reachability.
- `python3 -m py_compile tests/test_director_art.py tests/capture_director_art.py` and `git diff --check`: PASS.

Initial sandbox Chromium launch failed at macOS bootstrap permission; authorized isolated headless execution worked. During continuation the inherited4176 process stopped, producing one connection-refused run; restarted only this worktree's server. Harness errors were corrected: nonexistent `rq_leo_rescue`→actual `rq_leo_task`; explicit switch from ending to game; button-scoped local-tab selector because html also owns data-journey-mode; clean pages for camp fixtures to avoid residual UI state. These are not reported as production regressions. Earlier after screenshots predated the final dock specificity fix; all final after files are recaptured.

## Evidence, self-review and limits

Before: eight preserved320px screenshots under `before/`; broader baseline opening/city/camp evidence remains under the director artifact directory. Final matrix uses live4176 with no rev query at320×578,390×844 and1280×900. Explicit same-state fixtures, accelerated dialogue, normal settled animation; camp uses UI.restoreQaView only to switch screen then immediately removes qa-exact-replay. No animation-at-opacity-zero screenshots are claimed as final proof.

The walker disabling crop forces the first authored outcome through a temporary restored G.pickOutcome wrapper; it proves explicit scene selection, not an earned attack. Ridge/retreat/night outcomes and camp preparation/dialogue/back use real buttons. Full camp is a six-companion maximum-content fixture; it is not a newly earned campaign. Actual outcome return closes the modal. Selected meal disables the existing preparation action; empty and last-companion states are captured. No human reading-time, fun, physical-device performance or full-campaign acceptance claim.

Self-review checked source ownership, state descriptions, before/after distinction, cargo neutrality, absent-cast safety, active-vs-disabled machine state, metadata paths, budget, all-viewports action reachability and inherited-save scope. Story wording such as generic solo night language and unassigned ??? speakers remains explicit Task7 work. Existing pending finale/ordinary-scene reload concerns remain Task7/8; Task6 changes no save format. Generated CURRENT/asset-budget drift is excluded from the commit.

## Final narrow-camp review correction

After the initial63 capture run passed overflow/footer checks, vision review found a separate320 camp defect: two48px answer cards had a15.5px container, leaving only Back visible. This is why the earlier automated pass was insufficient. Added a camp-specific constraint within the existing narrative containment owner, reserving194px for the two answers and44px Back, and reducing only that decision state's art to120px. The story reader retains48px minimum and scrolls. A first attempt reserved rows alone but extended Back below the paper frame; the final rule also reclaims art height. The final camp-only matrix asserts at least96px answer-container height,44px individual targets, Back within viewport, and actual Back navigation. Controller review then caught a wrapped answer overlapping its cost. The overlap assertion also detected touching label/cost line boxes at390, so final camp cards at every width use intrinsic column flow,64px minimum,4px gap and a separate static cost row. A geometry assertion rejects actual rendered text-node line-box overlap between label and cost in every viewport (outer flex/span bounds include unused space; the final assertion measures text nodes). No gameplay state, narrative or cost changed.

The fixture now uses pending0, marks the already-seen event tutorial as seen, and waits for both ordinary toasts and `#quest-update-ribbon` to disappear naturally. The lingering quest notification was a separate ribbon, so waiting only for #toasts had not removed its occlusion. No UI is hidden to obtain final evidence. The15-frame camp-only matrix and18-state typography suite were rerun after the final change; unaffected earlier art crops remain valid.

## Final visual inventory and packaging

21 states at each viewport: postman solo; Leo rescue before; reset solo; uplink solo; core solo; night solo; night dawn result; ridge before and after; walker tutorial, disabled result, disabled choices, last action, retreat; opening choices; ending; camp empty, full, selected/disabled preparation, last companion and long dialogue. Personal vision review covered every new asset's actual crop, the320 ridge/postman/core/night/reset and action panels,390 walker disabled result/recruitment/camp, and desktop core/ending. The controller independently inspected the fresh320 retreat and empty camp plus both empty Seoul image deliveries.

Full63 after frames and eight inherited before frames remain under `qa-artifacts/director-art-2026-09-11/`; only representative copies, trace, logs, provenance and this report are committed in `audits/director-art-2026-09-11/`, following docs/CURRENT.md QA policy. `audits/README.md` linked from CURRENT was absent in this worktree, so no unavailable file is claimed as read. No strict visual warning threshold or file-size budget changed.

Final receipt: `capture_director_art.py --camp-only` passed15 refreshed camp frames at320/390/1280 with text-node label/cost separation and Back bounds; aggregate trace remains63 states. Latest typography suite passed18 states, source-health and py_compile/diff checks passed. Final390 and1280 camp dialog crops were also personally inspected.

## Review round1 — fixture identity and actual recruitment result

Base `f041fb065bc0eb2fff8a24ab611952ce90122118`; production mappings/CSS unchanged. Independent review correctly found that closing a previous result could schedule a normal Seoul chain450ms later, overwriting the alleged opening scene. The previous opening rows showed seoul-home-dawn-v2 and the committed320 image was 서울 권역 처리 완료. Those rows and representative are replaced; the rejected original trace/image remain locally under `qa-artifacts/director-art-2026-09-11/rejected-review-1/`, explicitly invalid opening evidence.

The opening and Leo rescue paths now each start in a fresh headless page; closing the old page discards its pending callbacks without suppressing or modifying any game chain. The default runner also gives its final ending fixture a fresh page. After normal1.3s settling and notification dismissal, the screenshot helper asserts and records actual `data-event-id`, title, scene key and phase before taking each relevant screenshot.

- Opening at320×578,390×844,1280×900: `opening_bus_repair` / 난방이 끊긴 가족 버스 / `intro-dock-aid` / event. All three choices exist. A second shot scrolls to the last choice and Playwright trial-click verifies pointer reachability without choosing it. The initial320 shot correctly shows the partially visible last row; the last-action shot proves it can be reached.
- Leo before at the same three viewports: `rq_leo_task` / 돌아가야 하는 이유 / `recruit-leo-rescue-choice-v1` / event. An explicit Namwon recruitment-task fixture supplies context. The actual third choice, 트럭을 물가까지 붙여 불빛을 만든다, is clicked. Result remains `rq_leo_task`, phase outcome, scene `recruit-leo-follow`; Leo and Bori are safely beside the parked Dalguji. Actual state assertions confirm van82→73 and75min elapsed. No outcome wrapper or direct result grant is used.
- The paired rescue and correct opening/last-action crops were visually inspected, including320,390 and desktop examples. Full12 fresh frames remain local; corrected opening320, its last-action320, and Leo before/after390 are committed as representative proof. Exact identities, resource receipts and all viewport states are in the updated trace; SHA-256 inventory is refreshed.

Covering command: `python3 tests/capture_director_art.py --review-only` — PASS12 fresh captures,69 states in aggregate inventory, zero page errors. This does not claim a fresh rerun of the57 unchanged retained states. Receipt: `audits/director-art-2026-09-11/review-1.log`. `python3 -m py_compile tests/capture_director_art.py` and `git diff --check` passed. No unchanged broad suites were repeated, no new production defect was found, and the existing58 asset warnings / size advisory and Task7 limitations remain as recorded above.
