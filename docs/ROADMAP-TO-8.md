# 전 영역 8점 로드맵 (2026-08-06 기준)

이 문서는 두 가지다: **어디를 어떤 순서로 고치는가(로드맵)**, 그리고
**같은 실수를 반복하지 않는 방법(공정)**. 후자가 먼저다 — 8/6 사이클은 구현량이 부족해서가
아니라 *잘못된 곳을 고치고, 게이트가 그걸 못 잡아서* 점수가 안 올랐다.

## 지금 위치 (독립 재검증 실측)

| 섹션 | 현재 | 8점까지 | 주된 병목 |
|---|---:|---:|---|
| 캐릭터·대사 | 8 | — | 유지 |
| 스토리·세계관 | 7 | +1 | 세계에 이빨이 없다 (gameover fx **0건**/894 이벤트) |
| 코어 루프 | 6 | +2 | **시간이 공짜** (시한 30일 vs 실완주 중앙 DAY 3~4) |
| 전투 | 6 | +2 | 48개 전투 선택 중 **21개(44%)가 판정 없는 확정** · 교전 5종뿐 |
| 성장·차량 | 6 | +2 | 카탈로그 608고철 vs 런당 수입 ~70 → 슬롯·중량이 안 물림 |
| UI/UX | 6 | +2 | 척도 중복 정리됨, 남은 건 접근성 마감과 정보 설계 |
| 비주얼 | 6.5 | +1.5 | 화풍 3종 혼재 · 달구지 외형이 컷마다 다름 |
| 오디오 | 6.5 | +1.5 | 음악 **2트랙**(주간/야간 주행)뿐 · iOS 볼륨 제어 미검증 |
| 아키텍처 | 5 | +3 | 파일은 나눴으나 단일 IIFE라 개별 파싱 불가 |
| 안정성·테스트 | 4 | +4 | 게이트가 자기 기능이 망가져도 통과했음 (4건 실증) |

숫자는 전부 이 리포지토리에서 재측정한 값이다(측정 명령은 각 항목 옆에 적었다).

---

# 파트 1. 공정 — 먼저 이걸 고쳐야 나머지가 지켜진다

## 원칙 1. 게이트는 자기가 지킨다는 걸 증명해야 한다

8/6 사이클의 검사 넷은 **자기가 이름 붙인 기능을 되돌려도 초록**이었다.
앞으로 새 검사를 추가할 때는 반드시 다음을 통과시킨다.

**뮤테이션 확인(Definition of Done의 일부)**
```
1. 검사를 작성한다 → 초록 확인
2. 그 기능을 코드에서 되돌린다(한 줄 주석 처리로 충분)
3. 검사가 빨간불이 되는지 확인 ← 안 되면 그 검사는 아직 검사가 아니다
4. 되돌린 것을 복구하고, 2~3에서 본 것을 검사 파일 주석에 남긴다
```

이번에 넣은 `tests/test_choice_visibility.py`, 야영 판정, 결정성 비교면 자체 검사가
이 형식의 예시다. 특히 **"비교면이 비어 있지 않은가"를 검사 안에서 확인**하는 패턴
(`out:` / `dawn:` 항목 수 확인)은 다른 검사에도 이식할 것.

## 원칙 2. 주장에는 재현 명령을 붙인다

결과 문서에 쓰는 모든 수치 옆에 **어떻게 다시 재나**를 적는다. 8/6 초판 문서의
"백미러 73→64"는 작업 중간값을 베이스라인으로 착각한 오기였고, 재현 명령이 있었으면
쓰는 순간 걸렸다.

```
❌ "목발 단어를 크게 줄였다"
✅ "한참 83→43 (git show <before>:src/03-data.js | grep -o 한참 | wc -l)"
```

실측(이미 일어난 것)과 전망(그럴 것)은 문장에서 분리한다.

## 원칙 3. 릴리스 전 적대적 재검증을 정식 단계로 둔다

이번에 효과가 증명된 절차다. 구현자가 아닌 리뷰어에게 **결과 문서를 "미검증 마케팅"으로
취급하고 코드로 대조**하게 시켰더니, 살아 있던 XSS·거짓 통과 게이트 4종·내가 만든 UI
회귀가 한 번에 나왔다. 자기 채점으로는 절대 안 나왔을 것들이다.

절차: 구현 → 자체 게이트 통과 → **적대적 재검증** → 반박 수정 → 게이트 재통과 → 커밋.
반박당한 주장은 문서에 표로 남긴다(지운다고 없던 일이 되지 않는다).

## 원칙 4. "배선됐다"와 "발동한다"를 구분해서 센다

DAY 30은 코드에 배선됐지만 실완주가 3~4일이라 날짜 축은 영영 안 닿았다.
새 시스템에는 반드시 **실엔진 시뮬에서 발동률**을 붙인다.

```
배선 확인:  단위 검사 (함수를 직접 호출)
발동 확인:  실엔진 시뮬에서 이 기능이 관여한 런의 비율
```
발동률이 낮으면 기능이 아니라 죽은 코드다. 신규 이벤트도 같은 기준으로 본다
(현재 갈등 아크 3.3% · 가짜 행정소 2.6% · 회수조 3.0% — 사실상 아무도 못 본다).

## 원칙 5. 한 사이클에 한 축만 건드린다

8/6에는 7개 Phase를 한 커밋에 넣어 무엇이 무엇을 깼는지 분리가 안 됐다
(전투 UI 회귀가 게이트를 통과해 버린 것도 이 때문). 앞으로는 웨이브당 한 축,
웨이브마다 게이트 통과 + 시뮬 리포트 갱신 + 커밋.

---

# 파트 2. 로드맵 — 웨이브 순서와 이유

순서의 논리: **키스톤 먼저.** 리뷰어 셋이 독립적으로 같은 지점을 짚었다 —
시간이 공짜라서 나머지 시스템이 전부 헐거워진다. 시한을 고치면 기존 이벤트
라이브러리 전체가 재평가되므로, 다른 밸런스 작업을 먼저 하면 두 번 하게 된다.

```
W1 시간 경제 (키스톤)  ──┬──> W3 전투 깊이
                          ├──> W4 경제·성장
W2 콘텐츠 전달 ───────────┘
W5 최종막
W6 아키텍처·테스트 (상시)
T  오디오·비주얼 (병렬 트랙, 외주/에셋 의존)
```

---

## W1 — 시간에 값을 매긴다 (키스톤)

**문제**: 시한 30일, 실완주 중앙 DAY 3~4. 배급·의뢰 기한·시한·피로가 전부 공짜라
"연료냐 왕진이냐" 같은 갈등이 도덕적 산수가 아니라 취향 선택이 된다.

**두 가지 방법이 있고, 하나를 고른다.**

| | A. 시한을 압축 | B. 하루를 비싸게 |
|---|---|---|
| 방식 | `D.transferDeadlineDay` 30 → **7** | 주행·사건·정비가 시간을 더 먹게 |
| 장점 | 데이터 한 줄, 즉시 검증 가능 | 서사(30일) 유지 |
| 단점 | 인트로·노트의 "30일" 문구 수정 필요 | 파급이 넓고 튜닝이 오래 걸림 |
| 권장 | **A로 먼저 실측**하고, 여유가 없으면 B로 미세조정 | |

**A안 작업 목록**
1. `D.transferDeadlineDay` = 7, 인트로·노트·`transferStatus` 문구 동기화
2. 압박 단계를 날짜 축으로 되돌린다(거리 축은 보조로만) — 지금은 거리 축이 무조건
   발동해 `deadlineSeenPct 100%`가 동어반복이다
3. 정착지 체류·탐색·수리가 반나절 이상을 먹도록 재조정 → 하루가 실제 선택이 되게
4. 초과 시 첫 이송이 **실제로 좌석을 가져간다** — 늦은 날수만큼 구제 불가 인원을
   `seoul_costs`·에필로그에 수치로 새긴다

**8점 방어 조건 (전부 실엔진 시뮬로 측정)**
- 정책 간 소요일 스프레드 **≥ 2일** (현재 1일)
- `direct` 정책의 시한 초과율 **≥ 30%**, `prepared`는 **≤ 10%**
- 완주 런의 중앙 DAY가 시한의 **60~90%** 구간에 들어온다
- 시한 초과 런과 준수 런의 엔딩 텍스트가 실제로 갈라진다(스냅샷 검사)

**영향**: 코어 루프 6→7.5, 스토리 7→7.5 (기존 이벤트가 재평가됨)

---

## W2 — 쓴 것이 보이게 한다 (콘텐츠 전달)

**문제**: 894 이벤트 / 풀 가중치 5,918 / 런당 사건 ~22건. 이번에 쓴 콘텐츠는
런당 등장률이 3% 안팎이라 **100명 중 3명만** 본다. 쓰는 것보다 전달이 병목이다.

**작업 목록**
1. **비트 승격**: 갈등 아크는 `w` 기반 랜덤이 아니라 조건 충족 시 큐에 넣는
   고정 비트로(딜리버리 이벤트·시한 이벤트가 쓰는 `queueStory`/`fixed` 방식)
2. **등장률 계측을 게이트에**: 주요 서사 비트 목록을 정의하고, 실엔진 시뮬에서
   각 비트의 발동률을 리포트 → 목표 미달 시 실패
3. **풀 위생**: 802개 풀 이벤트 중 오래된 저가치 이벤트의 `w`를 낮춰 신규·맥락
   이벤트가 뜨게 (지금은 신규가 노이즈에 묻힌다)
4. **악의 조우 3~4개 추가** — 2개로는 "대비"가 통계적으로 안 만들어진다

**8점 방어 조건**
- 지정한 핵심 비트 각각의 런당 발동률 **≥ 60%** (또는 명시적으로 "희귀" 태그)
- 한 런에서 만나는 서로 다른 이벤트 종류 수의 중앙값이 현재 대비 **개선**
- 악의 계열 조우를 한 번 이상 만나는 런 **≥ 70%**

**영향**: 스토리 7→8, 캐릭터 8 유지(이미 좋은 것이 보이게 됨)

---

## W3 — 전투를 결정으로 (깊이)

**문제**: 48개 전투 선택 중 **21개(44%)가 판정 없는 확정 결과**. 교전은 5종뿐이고,
모든 조우에 값싼 이탈구가 있다.

**작업 목록**
1. phase 1과 현장 임무 phase 2의 확정 선택에 판정을 부여하거나, 판정이 없다면
   **전망 표시를 떼고 "준비 행동"으로 명시** (지금은 확정 결과에 등급이 붙어 혼란)
2. **이탈에 값 매기기**: 지금은 연료 4·차체 3이면 어떤 교전이든 회피 가능.
   이탈은 관측 상승·동료 신뢰·해당 서사 훅 상실 중 하나를 지불하게
3. **관측(pursuit)에 임계 효과**: 현재 임계 분기가 사실상 없다(가중치 조정과
   몇몇 지역 이벤트 게이트뿐). 3에서 강제 검문, 5에서 정착지 거부/기둥 봉쇄
4. 교전 2종 추가(북부 외 지역에도) — 5종은 15단계라도 반복이 빨리 온다

**8점 방어 조건**
- 판정 없는 전투 선택 비율 **≤ 15%** (현재 44%)
- 이탈 선택률이 정책 무관 **≤ 40%** (지금은 사실상 최적해)
- pursuit 3·5에서 실제로 다른 사건이 발생함을 시뮬로 확인
- 전투 결과 분포가 성공/부분/실패 **세 갈래 모두 10% 이상**

**영향**: 전투 6→8

---

## W4 — 경제가 물리게 (성장)

**문제**: 카탈로그 608고철 + 부품 22 vs 런당 실수입 ~70고철. 슬롯·중량은 코드로는
동작하지만 도달 가능한 빌드가 없어 **기회비용이 발생하지 않는다**(런당 4~5개 장착).

**작업 목록**
1. 수입과 카탈로그의 척도를 맞춘다 — 수입 3배 인상이든, 카탈로그 재가격이든,
   런 길이 연장(W1과 연동)이든 **하나를 골라 실측으로 확인**
2. 중량 문턱(현재 연비 8pt·마모 10pt)을 현실 빌드 범위(7~10pt)로 내린다 —
   지금은 문턱이 도달 불가라 사실상 꺼져 있다
3. 동료 영입을 시뮬 정책에 넣는다(현재 `party: 0` — 성장 축 전체가 미측정)
4. 퍼크 A/B 선택 분포를 시뮬로 관측해 명백한 우열쌍을 마저 정리

**8점 방어 조건**
- 런당 장착 업그레이드 중앙값 **8~12개** (현재 4~5)
- 슬롯 경합이 실제로 발생한 런 **≥ 50%**
- 4개 빌드 아키타입의 연비·소요일·완주율이 **유의미하게 분화**
- 시뮬 `meanParty` **≥ 3**

**영향**: 성장 6→8, 코어 루프 7.5→8

---

## W5 — 최종막을 결정으로

**문제**: `seoul_costs`는 선택지가 **1개**인 페이지 넘김이고, 세 처분에는 요구 조건·
실패·자원 비용이 없다. 대가 극화는 됐지만 "저항"은 과장이었다.

**작업 목록**
1. 세 처분에 **요구 조건**을 건다 — 인계는 실제로 이은 거점 수, 보존은 야간 근무를
   받아들일 동료(낮은 사기면 거부), 수면은 열쇠를 나눠 맡을 상대
2. 충족 못 한 처분은 **선택지에서 잠기고 이유가 보인다**(지금은 전부 열려 있음)
3. `seoul_costs`를 실제 선택으로 — 무엇을 먼저 확인할지 고르고, 고른 것만 알게 된다
4. 동료 반대를 누르면 **비용을 지불**한다(유대 하락·에필로그 기여 상실)
5. **패배 엔딩 작성** — 현재 authored 엔딩은 갈증 하나뿐. 두 번째 이송 이후 도착은
   승리 텍스트의 변주가 아니라 이름 있는 엔딩이어야 한다

**8점 방어 조건**
- 준비 상태에 따라 잠기는 처분이 실제로 발생(시뮬 확인)
- 엔딩 종류 **≥ 5** (처분 3 × 시한 준수/초과 + 패배)
- 반대 무시가 에필로그 텍스트를 바꾸는 것을 스냅샷 검사로 고정

**영향**: 스토리 8→8.5, 캐릭터 8 유지

---

## W6 — 아키텍처·테스트 (상시, 웨이브마다 조금씩)

**문제**: 파일은 5분할했지만 **단일 IIFE라 개별 파싱이 안 된다**(`node --check` 실패).
게이트는 이번에 고쳤지만 구조적으로 더 가야 한다.

**작업 목록**
1. UI를 진짜 모듈로 — 파일별 IIFE + 명시적 인터페이스 객체 교환.
   완료 기준: **모든 파일이 `node --check` 단독 통과**, 한 파일에서 선언하고 다른
   파일에서 변이하는 식별자 0
2. `04-engine.js`(3,000줄+)를 이미 달린 섹션 주석대로 분할
3. 선택에 영향 주는 모듈 변수를 전부 `S`로 이동 — `resetDriveTimers` 같은 리셋
   호출을 늘리는 대신 구조로 해결
4. 세이브 픽스처를 **실제 아카이브 JSON**으로 교체(현재는 최신 세이브를 깎아 만들어
   내일 추가되는 필드는 자동으로 "옛 세이브"에 들어간다)
5. 테스트가 빌드에 의존하게(현재 구 빌드 대상으로 초록 가능) + 락파일 커밋
6. CI를 실제로 돌려 초록 만들기 — 지금은 커밋만 되어 있고 **한 번도 실행 안 됨**

**8점 방어 조건**
- 파일 단독 파싱 100%, 파일당 ≤ 1,200줄
- 뮤테이션 확인 통과한 검사 비율 100%(새 검사 기준)
- CI 초록 이력 존재, `pages.yml`이 게이트에 의존
- 테스트 실행 전 자동 빌드

**영향**: 아키텍처 5→8, 안정성 4→8

---

## T — 오디오·비주얼 (병렬 트랙)

이전 사이클에서 제외했지만 **"전반적으로 8"에는 반드시 포함**된다. 코드 작업과
독립이라 병렬 진행 가능하고, 에셋 생성 비용이 주 변수다.

**비주얼 6.5 → 8**
1. 화풍 하나로 고정 — 픽셀 계열이 타이틀·HUD·달구지 렌더러와 정합
2. **달구지 표준 참조 시트**를 먼저 만들고, 차가 등장하는 컷 전부 재생성
   (지금은 컷마다 베이지 봉고 / 노란 밴 / 올리브 캠퍼로 변신)
3. 설정 위반 컷 교체(수원 화성의 중세 궁수, 한국이 아닌 침엽수 고속도로)
4. 98장 중 상위 20장만 재생성 — 새 컷 추가보다 연속성이 먼저

**오디오 6.5 → 8**
1. **음악 2트랙 → 5트랙** (정착지 / 긴장·전투 / 남산 피날레 추가)
2. 모든 MP3를 AudioContext로 라우팅 — 현재 `HTMLMediaElement.volume` 기반이라
   **iOS에서 믹서·크로스페이드가 통째로 무력화될 수 있다**(실기 미검증)
3. 실기 청취 매트릭스(스피커·이어폰·전화 스피커) 1회 수행
4. 장소 정체성을 볼륨 차이가 아니라 실제 다른 소스로

**8점 방어 조건**
- 달구지가 등장하는 모든 컷에서 차량 실루엣·색이 동일(사람 검수)
- 무작위 컷 20장에서 화풍 판별 불가(블라인드)
- iOS 실기에서 채널 슬라이더가 실제로 동작함을 녹화로 확인
- 음악이 장면 종류마다 바뀌는 것을 실기 청취로 확인

---

# 파트 3. 웨이브 운영 방식

각 웨이브는 동일한 5단계를 밟는다.

```
1. 착수 전  — 그 웨이브의 방어 조건을 먼저 게이트에 넣는다 (빨간불로 시작)
2. 구현     — 한 축만
3. 자체 검증 — 게이트 초록 + 실엔진 시뮬 리포트 갱신
4. 적대적 재검증 — 다른 리뷰어에게 "미검증 마케팅" 취급으로 코드 대조
5. 반박 수정 → 게이트 재통과 → 커밋 (반박 내역은 문서에 표로 남김)
```

**1번이 핵심이다.** 방어 조건을 먼저 빨간불로 만들어 두면, 구현이 끝났는지를
사람이 판단하지 않는다. 8/6에는 이걸 나중에 붙여서 "통과하도록 쓴" 검사가 됐다.

## 진행 관리

- 웨이브마다 `reports/engine-simulation.json` 스냅샷을 남겨 추세를 본다
- 점수는 웨이브 종료 때마다 **적대적 재채점**으로만 갱신한다(자기 채점 금지)
- 미해결로 남긴 것은 문서에 남긴다 — "안 했다"와 "못 봤다"는 다르다

## 순서 요약

| 웨이브 | 축 | 선행 | 주 영향 |
|---|---|---|---|
| W1 | 시간 경제 | — | 코어 6→7.5, 스토리 +0.5 |
| W2 | 콘텐츠 전달 | — (W1과 병렬 가능) | 스토리 →8 |
| W3 | 전투 깊이 | W1 | 전투 6→8 |
| W4 | 경제·성장 | W1 | 성장 6→8, 코어 →8 |
| W5 | 최종막 | W2 | 스토리 →8.5 |
| W6 | 아키텍처·테스트 | 상시 | 5→8, 4→8 |
| T | 오디오·비주얼 | 병렬 | 6.5→8 ×2 |

W1과 W2는 서로 독립이라 동시에 갈 수 있다. W3·W4는 W1의 시간 척도가 정해진 뒤라야
튜닝이 두 번 되지 않는다. W6는 매 웨이브에 조금씩 얹는다.

---

## 부록 — 이 문서의 숫자를 다시 재는 법

```sh
# 이벤트·선택·전투 확정 비율·등장률·카탈로그 비용
node -e "const fs=require('fs');const D=new Function(fs.readFileSync('src/03-data.js','utf8')+'\nreturn D;')(); /* 본문 참조 */"

# 실완주 중앙 DAY·완주율·구제 횟수·동행 수
npm run simulate:engine

# 목발 단어 추이
git show <ref>:src/03-data.js | grep -o 한참 | wc -l
```

측정일: 2026-08-06. 이후 수치가 달라지면 **이 문서를 고치고 측정일을 갱신한다**
(옛 숫자를 그대로 인용하는 것이 8/6 사이클의 실패 중 하나였다).
